name: Publish to npm

on:
  push:
    branches:
      - main

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org

      - name: Show Node and npm versions
        run: |
          node --version
          npm --version

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test

      - name: Create CI version
        shell: bash
        run: |
          BASE_VERSION=$(node -p "require('./package.json').version")
          CI_VERSION="${BASE_VERSION}-main.${GITHUB_RUN_NUMBER}"
          npm version "${CI_VERSION}" --no-git-tag-version

      - name: Publish package
        run: npm publish --tag main --access public
